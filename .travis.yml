language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

before_script:
  - travis_retry composer self-update
  - travis_retry composer update --prefer-source --no-interaction --prefer-stable

script: vendor/php/phpunit -c ./bootstrap/phpunit.xml